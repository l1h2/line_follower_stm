#ifndef TRACK_SELECTOR_H
#define TRACK_SELECTOR_H

#include <stdint.h>

extern const float* const waypoints_x;  // Array of x-coordinates of waypoints
extern const float* const waypoints_y;  // Array of y-coordinates of waypoints
extern const uint16_t waypoint_count;   // Number of waypoints

#endif  // TRACK_SELECTOR_H
